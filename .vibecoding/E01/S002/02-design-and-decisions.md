# 02 - 规范设计与决策

> Thread: 工具设计方法论与 grep_search 具体决策
> 日期: 2026-03-16

---

## Round 1: 从参数上升到方法论

**🧑 User:**

> 我觉得是这样，因为还有一些其他的工具。
> 我希望在这里，不仅可以知道如何实现功能，还需要额外知道了解设计工具的基本思路。当我要从零设计这个工具的时候，这个基本思路也很重要。
> 所以我们要按照我们的思路，去进行这个调试和设计。

**🤖 Agent:**

提出 Agent 工具设计的四个核心问题：

1. **工具解决什么问题？**（已有 D03）
2. **LLM 需要控制什么，什么应该自动化？** — 决定参数设计
3. **工具返回什么，LLM 拿到后怎么用？** — 决定输出格式
4. **边界情况怎么兜底？** — 截断、超时、错误处理

---

## Round 2: 人机对称原则

**🧑 User:**

> 是的，我想都用一个基本思路，但不同工具肯定有它特异性的地方。我们在设计工具的同时，也要让大家知道工具是干什么的，以及如果要设计工具，它该是怎么运转的，会怎么去想、怎么去设计。
>
> 关于 Agent 使用工具，其实还有一个我想说的原则：
> 1. 对人好用的工具，对 AI 也好用。
> 2. 对 AI 好用的工具，反过来一般来说对人也好用。
>
> 所以当你设计一个工具或者一个 API，人用不明白的时候，大概率 AI 也很容易出错。
>
> 对，这里有两个核心问题：
> 1. 第一个确实是工具到底要解决什么问题。
> 2. 第二个是 LLM 需要控制到什么程度，以及什么应该自动化。
>
> 我不太理解的是，工具返回什么、LLM 拿到后怎么用，这确实需要从那个视角来看。然后边界情况怎么兜底？是的，这个也是需要的。甚至边界情况我们可以分几类

**🤖 Agent:**

修正「LLM 特殊论」为「好设计通用论」—— 对人好用的工具对 AI 也好用，反之亦然。

展开 Q3（输出契约）：grep_search 在工具链中的位置是「定位 → 精读」，返回信息应刚好够 LLM 判断下一步 read 哪个文件的哪几行。

边界情况分类建议：量的边界、质的边界、资源边界。用户表示可先放一放。

---

## Round 3: Story 三层学习结构

**🧑 User:**

> 我想的是这样，每一个 story 都能学到几个点：
> 1. 学到某一个重要的组件、部件功能如何开发，以及如何实现。
> 2. 每个 story 都会学到一个新的思考点、思维点和思维角度。
> 3. 会有很多 deep dive 的深度话题。
>    (a) 这些话题我们可能不会立刻实现，但是可以先列下来，也许未来会去实现。
>    (b) 虽然现在实现的是简版，但我们需要考虑这个。
>
> 所以我们之前说了，前面有个扩展阅读的内容。扩展阅读里面有些可能是目前不实现的，那么初步上手的人就可以通过看代码，发现问题并去找到自己思考的答案，未来我们也会去做对应的实现。
>
> 然后关于边界情况的处理，我觉得可以先放一放。因为我们之前说过，目前的版本还是比较基础的，我们可以把这部分内容放到扩展阅读里面。

**🤖 Agent:**

确认 Story 三层学习结构，边界情况推迟到扩展阅读。沉淀 D04（方法论）、D05（边界情况推迟）。

---

## Round 4: 参数设计

**🧑 User:**

> Q2 把参数设计

**🤖 Agent:**

从「使用者需要控制什么」推导，而非从竞品参数出发：

- **搜什么** → `pattern`（必填）
- **在哪搜** → `path`（可选，默认项目根）
- **搜什么类型文件** → `include`（可选）

自动化：`.gitignore` 遵守、结果截断、搜索引擎选择。

**🧑 User:**

> 我觉得上下文、函数需要暴露，这个也是来自于我自己的使用经验。但是，这个东西是另一个话题里的，还不是这里的。

**🤖 Agent:**

确认 context 归入 Q3 讨论。

**🧑 User:**

> 放到 Q3 里讨论。然后类比我在 VS Code 里的搜索功能，大家在 VS Code 里的常用习惯中，我觉得「排除匹配」这个功能也比较重要，也就是反向筛选。

**🤖 Agent:**

区分两种排除：排除文件 vs 排除匹配行。用户确认与 VS Code 一致，做**文件级 exclude**。

**🧑 User:**

> 好的，与 vscode 一致

**🤖 Agent:**

参数设计定版：`pattern`（必填）+ `path` / `include` / `exclude`（可选）。沉淀 D06。

---

## Round 5: 输出格式

**🧑 User:**

> 好的，我们继续。整体上，我还是从使用者或人机交互的角度出发。搜索的话，我希望能看到一些相关内容，并加上文件路径以及内容在代码里的位置。
>
> 为什么需要这个代码里的行号位置呢？因为我假定一个使用场景：我先搜索到信息，再去读取。那么我可能需要读取对应的范围，因为我搜到了对应的范围。所以这是一个结合的、一系列的使用场景，它肯定需要能知道关键片段在文件里的位置，这将有利于按需读取。
>
> 关于输出格式，你有什么建议吗？我觉得可以这样：因为我们的原则是「人能看懂，AI 也能看懂」，所以你可以提供几个重要的、可行的格式给我，我来确认和选择。

**🤖 Agent:**

给出三个方案：A（OpenCode 风格）、B（grep 传统风格）、C（Gemini CLI 风格）。对比维度：路径风格、文件归属、行号格式、token 效率、可读性。

**🧑 User:**

> 我倾向 Gemini CLI 这种

**🤖 Agent:**

确认输出格式。context 参数：暴露还是固定？

**🧑 User:**

> 支持参数 /discuss-for-specs

**🤖 Agent:**

context 暴露为参数，5 个参数。默认值 0 vs 2-3？用户确认：默认 0，LLM 需要时主动指定。简单截断策略。沉淀 D07、D08。

---

## Round 6: 技术选型

**🧑 User:**

> 系统有 rg 就用，没有就自动下载

**🤖 Agent:**

确认 ripgrep，系统优先，fallback 自动下载。

**🧑 User:**

> 你下看一下，像这个 ripgrep 是从哪去下载的？会不会有网络限制，或者是什么时机下载的？

**🤖 Agent:**

从调研提取：四家都从 GitHub Releases 下载；国内网络可能不稳定；首次调用时懒加载。

**🧑 User:**

> 是否可以我预先内置，然后根据不同的操作系统版本使用不同的 ripgrep 包看一下呢？

**🤖 Agent:**

给出三种预内置方案：1) 二进制进仓库；2) npm 平台 optional；3) `@vscode/ripgrep`。建议方案 3。

**🧑 User:**

> 可以，我觉得暂时先按方案 3，因为我们还不需要构建发布。但如果我们要构建发布的话（打包发布的话），是不是可能就需要方案 1 这种？

**🤖 Agent:**

确认 MVP 用 `@vscode/ripgrep`，发布分发的策略切换放入扩展阅读。沉淀 D09。

---

## Round 7: 排序、截断、工具描述

**🧑 User:**

> 你看一下，作为讨论工具，咱们还有什么需要讨论的吗？看工具里，它不是还有一些像什么参数设计我们做完了，输入格式也做完了，不是还有什么排序策略、然后截断策略之类的，就这些需要去讨论吗？

**🤖 Agent:**

盘点剩余项：排序（建议不排）、截断（100 条）、工具描述（模板字符串）。

**🧑 User:**

> 可以修改，就按修改时间来，然后简单截断也可以。

**🤖 Agent:**

确认：排序按修改时间降序，截断 100 条，工具描述用模板字符串。沉淀 D10。

---

## 关键决策摘要

| 编号 | 决策 |
|------|------|
| D04 | 工具设计方法论：四个核心问题 + 人机对称原则 |
| D05 | 边界情况推迟到扩展阅读 |
| D06 | 参数：pattern/path/include/exclude |
| D07 | 输出格式：Gemini CLI 风格 |
| D08 | context 参数：默认 0 |
| D09 | 技术选型：@vscode/ripgrep |
| D10 | 排序（修改时间）、截断（100 条）、描述（模板） |
